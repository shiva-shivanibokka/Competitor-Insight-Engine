# URL of the backend API (the FastAPI service on Hugging Face Spaces).
# Set this in Vercel → Project → Settings → Environment Variables.
# For local dev with the backend running locally, use http://localhost:7860
NEXT_PUBLIC_BACKEND_URL=https://your-space.hf.space
